import type {ReferenceDataProposalFamily} from "./types";

/** Pricing and market: pricing observations and curves, sample quality, communication width, regime, premiums, tenor, size and liquidity, indexer basis, costs, mandates and distribution waves. */
export const pricingMarketProposals: ReferenceDataProposalFamily = {};
