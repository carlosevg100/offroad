import type {ReferenceDataProposalFamily} from "./types";

/** Intake, materials and quality control: request batches, archetype requirements, question sets, rounding, QC tolerances and red flags. */
export const intakeMaterialsQcProposals: ReferenceDataProposalFamily = {};
