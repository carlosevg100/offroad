import type {ReferenceDataProposalFamily} from "./types";

/** Debt views and scenarios: debt reconciliation, maturity and renewal, business-plan and market scenarios, headroom, transaction sizing, costs, disbursement, mixed use and waiting analysis. */
export const debtScenarioProposals: ReferenceDataProposalFamily = {};
