import type {ReferenceDataProposalFamily} from "./types";

/** Financial analysis: reconciliation, materiality, normalization, cash-flow bridge, capex, revenue quality, related parties, seasonality, currency, receivables, concentration and peer ranges. */
export const financialAnalysisProposals: ReferenceDataProposalFamily = {};
