{
  "name": "@offroad/release-governance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude 'dist/**'",
    "program-board:render": "node --experimental-strip-types scripts/render-endgame-program-board.ts",
    "security-inventory:render": "node --experimental-strip-types scripts/render-security-current-state.ts"
  },
  "dependencies": {
    "@offroad/case-runner": "workspace:*",
    "@offroad/case-understanding": "workspace:*",
    "zod": "4.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43"
  }
}
