{
  "name": "@offroad/testing-fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "camil": "tsx scripts/build-camil.ts",
    "fakeco": "tsx scripts/build-fakeco.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude 'dist/**' --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.19.43"
  },
  "dependencies": {
    "jszip": "3.10.1",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  }
}
