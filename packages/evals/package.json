{
  "name": "@offroad/evals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "baseline": "tsx src/cli.ts baseline",
    "build": "tsc -p tsconfig.json",
    "fakeco:verify": "tsx scripts/verify-fakeco.ts",
    "gold:rede-horizonte": "tsx scripts/build-gold-rede-horizonte.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "measure": "tsx scripts/measure-extraction.ts",
    "measure:classification": "tsx scripts/measure-classification.ts",
    "measure:classification:local": "tsx --env-file=../../.env.local scripts/measure-classification.ts",
    "measure:local": "tsx --env-file=../../.env.local scripts/measure-extraction.ts",
    "test": "vitest run --exclude 'dist/**'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@offroad/credit-ontology": "workspace:*",
    "@offroad/document-classification": "workspace:*",
    "@offroad/document-extraction": "workspace:*",
    "@offroad/document-intelligence": "workspace:*",
    "@offroad/document-parsers": "workspace:*",
    "@offroad/model-gateway": "workspace:*",
    "@offroad/testing-fixtures": "workspace:*",
    "decimal.js": "10.6.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "tsx": "4.23.12"
  }
}
