/** The longest question or objective an execution request accepts: the request action validates
 * each one against it, and a follow-up is offered as an objective only when its text fits. */
export const executionRequestTextMaxLength = 2000;
