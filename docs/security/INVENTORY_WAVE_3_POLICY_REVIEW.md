# Wave 3: stage 3 policy review

The authorized scope is stage 3. Stage 5 and administrative screens remain outside this wave. The governed opening inventory remains pinned to its immutable baseline; this review records the material delta before the implementation merge. The final delivery review now pins merged implementation a8cddded and refreshes evidence hashes; see history/wave-3-final-review.md.

Controls affected: TRUST-ID-01, TRUST-APP-01, TRUST-APP-02, TRUST-DATA-01, TRUST-DATA-03 and TRUST-OPS-01. The six new private tables contain authority identifiers, membership validity and barrier configuration. Their RLS is forced, all four direct client operations deny access, and only bounded commands derive the active organization. The shared TypeScript package validates wire contracts; it cannot grant access.

Reviewed threats: cross-organization group assignment, forged workspace context, nested group cycles, deny overridden by another desk, administrator implicitly reading content, hidden object enumeration, expired membership, transferred worker capability, worker scope expansion, revoked job resurrection, export through legacy Storage, and legacy revocation leaving a group path. Synthetic rollback tests exercise these cases in staging. The eight common vectors run in SQL and TypeScript. Existing fixtures now explicitly bind the simulated worker account instead of relying on transferable bearer authority; original assertions remain.

Publication and policy mutation share an organization authorization lock. Mandatory policy denies are checked under that lock before a legacy grant upsert. Purpose changes, configuration changes and grants write private audit/outbox events in the mutation transaction; failure aborts the command. Existing synchronous RLS and job checks remain authoritative while the outbox propagates.

Compatibility PR 628 adds only the access_policy aggregate kind to the bounded event envelope. Its production consumer precedes the migration. No IAM, provider, subprocessor, retention policy or external transmission changes. No production fixtures or copied customer data are used. Historical assurance gaps remain open; the implementation does not claim independent audit or broad effective-IAM verification. This review is the implementing agent's technical review, not an external attestation.

Installed verification on 16 September 2026: 67 staging SQL contracts PASS; three migration SQL hashes agree; 53 function definitions agree between staging and production; 75 new catalogue surfaces reviewed; security advisors report zero lints. Duplicate purpose capture reproduced as two events and corrected by immutable successor, with one-event/no-op regression. Production verification reads metadata and existing aggregate state only. Runtime delivery is observed at ECS revision 338, exact image a8cddded, healthy active-task polling and four alarms OK. The delivered inventory links 63 verified evidence references; opening snapshots remain in history. The external completion report also records delivery of this final reconciliation.
