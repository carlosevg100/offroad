# Revisão final técnica da onda 5

A onda autorizada entrega somente a etapa 6: fonte lógica, versão imutável e vínculos explícitos. PR 635, baseline `3ccc8bcd4c399f984cd399daadf063f1e54638d7`. Nenhuma etapa posterior foi iniciada. O inventário vincula 84 evidências a esse commit e às observações operacionais delimitadas. O achado `SG-SOURCE-VERIFICATION-AUTHORITY` está corrigido; as 18 lacunas gerais conservam severidade e tratamento em `docs/build/arcabouco/RISCOS-POR-INCREMENTO.md`. A revisão não declara prontidão enterprise nem amplia autoridade de execução.

Duas migrações: staging `20260916211117`/`20260916211402`, produção `20260916212202`/`20260916212209`, SQL idêntico nos journals. 27 funções em paridade; 69 contratos SQL passaram no candidato e no schema instalado de staging, com rollback. Replay completo e checkers passaram na CI. 323 arquivos de migração têm carimbo em produção; advisors de segurança sem lints. Produção preserva 28 documentos/fontes/versões/vínculos, 24 perfis e 24 camadas, sem divergências de byte-identidade. Os 17 carimbos antigos são histórico não comprovado; nova confirmação requer leitura real pelo worker autorizado.

A mesma reprodução sintética primeiro alterou hash e confirmação sem grant; após a instalação recebe `42501` na RPC antiga. O novo contrato nega recibos forjados, troca de versão, outro tenant, sobrescrita de bytes e acesso revogado. Download revalida finalidade e autoridade após I/O. Remover vínculo não destrói outra utilização. A inspeção PDF distingue objetos executáveis de bytes de imagem e limita expansão, profundidade, quantidade e estruturas xref; seis regressões preservam o corpus real.

Quality da PR 635 `35156135064` e Security `35156135049` passaram, assim como o preview. O E2E teve 30 jornadas na primeira tentativa e uma na repetição (16 testes externos desativados pela configuração local). A corrida foi identificada no teste de revogação: o aviso salvo da concessão anterior já estava visível. Esta conciliação espera a remoção do grant na resposta do servidor antes de exigir 404 imediatamente, sem polling da autorização. Seu próprio E2E precisa passar antes do merge; o recibo externo registra o resultado final. Não há relaxamento de autorização ou teste negativo.

Quality de main `35157109817` e Security `35157109517` passaram. Vercel Production `6491529123` e ECS revisão 344 executam `3ccc8bcd4c399f984cd399daadf063f1e54638d7`; rollout completo, uma tarefa ativa e polling atual. Página pública 200 e rotas privadas redirecionam ao login. Verificações de produção somente por leitura, sem fixtures. A observação AWS não comprova IAM independente: a negativa histórica permanece, e quatro alarmes OK ainda não têm ação de notificação. O risco tem tratamento obrigatório na etapa 18 antes da continuidade.

A abertura do inventário e sua observação AWS ficam preservadas no histórico. O novo teste exige as 14 evidências adicionais e nega sua remoção. Os hashes são calculados sobre os bytes do baseline mesclado. Os recibos do commit desta conciliação serão preservados no completion externo, evitando declarar um commit futuro implantado.

Ficam para seus incrementos: direitos e busca (7), inputs privados entre dossiês/dependências/continuidade (17/18), retenção/purge (22), Temporal, conectores e ensaios de produto. O próximo início requer OK do fundador. Não foi criado outro ato exclusivo do fundador.

A CI da conciliação revelou a projeção de revogação incorreta: o trace continha respostas POST 200 com result=saved, mas a linha remanescente era o tombstone deny, apresentado sem efeito como concessão. A hipótese inicial de cache não explicava o resultado. A migração `workspace_access_allow_projection` limita a lista legada a allow, preservando a negação no avaliador. Staging `20260916230213`, produção `20260916230240`, SQL MD5 `06781189b20c7ec496fbd6b78de58c1d`, definição MD5 `860085373ee51ef03fee559b4dd3a813` idênticos. A mesma reprodução: antes `can_read=false/listed=true`; depois `false/false`. Advisors sem alertas, tipos de produção idênticos e checkers aprovados com 324 migrações de produção. Prova em `docs/build/arcabouco/etapa-06-access-projection-eval.json`.

A ação revalida a página após mutação bem-sucedida e o E2E aguarda o grant desaparecer, depois exige 404 imediatamente. O novo SQL `workspace_access_projection.sql` prova concessão visível, revogação ausente da lista e tombstone deny preservado. Nenhum privilégio foi relaxado; não há dados de teste em produção.

Os 70 contratos SQL foram repetidos no schema final instalado de staging e passaram, com rollback. A verificação posterior encontrou zero documentos remanescentes. Nomes e horários estão em `etapa-06-access-projection-eval.json`; não houve fixture em produção.
