# Revisão final técnica da onda 4

A onda autorizada entrega a etapa 5: identidade de entidade e dossiê privado, com memória pública comprovada. O baseline mesclado é `aee07fc000d27bdaf08c0b123a66e271c3d7f2d3` (PR 632). O fundador aprovou concluir esse recorte sem antecipar 17/18: o candidato de memória privada entre dossiês foi retirado antes de aplicação. Verificar permissões humanas não amplia a delegação do worker. O loader relacionado permanece vazio, com regressão que impede sua reativação nesta etapa.

A revisão vincula 70 evidências ao commit mesclado, incluindo sete novas de schema, isolamento, cache público, contratos, worker, eval instalado e instalação. As relações de Supabase, worker, Postgres, fluxo web-banco, fluxo banco-worker e identidade do worker foram revistas. As 18 lacunas gerais preservam severidade e tratamento; não se infere certificação ou readiness enterprise. O snapshot ativo permanece `wave-4`, aberto apenas para esta entrega, até novo OK do fundador e renovação da próxima onda.

Migração staging `20260916190433`, produção `20260916190600`, mesmo SQL no journal. Quatro tabelas, 56 superfícies catalogadas e 31 funções em paridade. Os 68 contratos SQL passaram com candidato e no schema instalado; o replay limpo na CI também passou, incluindo o checker de cada versão de arquivo no journal de produção. Advisors de segurança sem lints nos dois ambientes. Produção foi verificada somente por leitura: 50 recursos e 50 dossiês, zero faltas/desvios, nenhuma entidade inferida e nenhum dado descartável.

A CI da PR 632 (`35139348910`) aprovou qualidade, banco e 31 jornadas E2E; os scans (`35139348914`) e o preview passaram. A Vercel Production `6488774421` corresponde ao baseline. O deploy worker `35140654745` passou: ECS revisão 341, imagem `document-worker:aee07fc000d2`, uma tarefa ativa, rollout completo e polling atual sem bloqueio/pendência. Página pública 200 e rotas privadas anônimas redirecionam ao login. Quality de main `35140654876` e Security `35140654734` também passaram. O commit da própria conciliação tem recibos separados no completion externo da etapa.

A observação AWS é limitada às leituras descritas, sem atestação independente de IAM. A negativa OIDC histórica permanece registrada; o login do operador não a converte em permissão comprovada. Os quatro alarmes estão em OK, ainda sem ações de notificação. Recomenda-se configurar destinos e verificar permissões de diagnóstico no trabalho operacional correspondente; essas lacunas não foram declaradas encerradas.

O novo teste de governança exige as sete evidências e rejeita sua retirada. Permanecem as negativas de hash, origem, coletor, datas, onda fechada/desconhecida, expiração e alegações de certificação. Snapshot e observação de abertura estão preservados no histórico. O render final deriva do avaliador confiável e dos bytes imutáveis do baseline.

Fora da etapa: entradas adicionais e dependências de execução 17/18, reutilização privada entre dossiês, Temporal, conectores, telas administrativas e ensaios de produto. O registro público não foi povoado por inferência: requer fonte oficial, hash e publicação operacional revisada. Identidade incerta conserva a pesquisa por consulta, sem reutilização de memória por companhia. A próxima etapa depende de novo OK.
