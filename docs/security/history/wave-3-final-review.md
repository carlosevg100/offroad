# Revisão final técnica da onda 3

A onda autorizada entrega a etapa 3: política comum de acesso, grupos planos, barreiras, finalidades e delegação limitada. A implementação está no baseline mesclado `a8cdddedda7ad1d09915571d54e98a775c42763a`. O fechamento técnico não autoriza a etapa 5 ou uma nova onda. O snapshot ativo permanece `wave-3`, aberto e revisado, até o OK do fundador e a renovação atômica da onda seguinte.

A revisão vincula 63 evidências, incluindo nove novas evidências de implementação, isolamento, delegação, exportação, conformidade e eval instalado. As relações com web, Supabase, worker, banco, objetos, fluxo web-banco, fluxo banco-worker e identidade do worker foram revistas. Os 18 gaps anteriores conservam severidade, alvos e tratamento. A autoridade administrativa não concede leitura; Postgres decide e o contrato TypeScript não contém outro motor de autorização.

As três migrações têm carimbos de produção `20260916163753`, `20260916163756` e `20260916163759`; staging usa `20260916124024`, `20260916124447` e `20260916163357`. O SQL é idêntico entre os journals. Os 67 contratos SQL passaram no schema instalado em staging; 53 funções coincidem por hash. O inventário revisa 75 superfícies novas, conserva a diferença histórica conhecida de staging e os checkers de catálogo e histórico passaram. Advisors de segurança: zero lints nos dois ambientes. Nenhum dado descartável foi criado em produção.

O gate da PR 629 executou qualidade, migrações do zero, testes SQL, concorrência de transferência de autoridade e 31 E2Es com sucesso. A consulta sintética de 200 grupos e 200 amostras apresentou p95 de 10,0992 ms em staging, abaixo do limite de 100 ms; isso não representa SLO de carga de clientes. A duplicação de evento de política foi reproduzida com dois eventos antes, corrigida por migração sucessora e testada com um evento após mudança e nenhum adicional após retry sem alteração.

O snapshot e a observação de abertura ficam preservados no histórico. A observação final AWS vincula a imagem e o polling da tarefa ativa ao baseline entregue; permanece uma observação operacional sem autoridade de atestação independente. A negativa OIDC histórica não foi convertida em permissão comprovada. Os quatro alarmes continuam sem ações de notificação. A recomendação é configurar os destinos e verificar a política IAM no trabalho operacional correspondente, sem declarar esses gaps encerrados nesta etapa.

O novo teste de governança recusa a retirada das nove evidências da etapa. Permanecem as negativas de adulteração de hash, origem, coletor, datas, onda fechada/desconhecida, expiração e claims de certificação. O render final é produzido pelo avaliador confiável, com verificação dos bytes no commit mesclado e da observação coletada.

O baseline entregou Quality de main `35124886036`, Security `35124885842` e deploy worker `35124885802` com sucesso. ECS revisão 338 opera a imagem `document-worker:a8cdddedda7a` com uma tarefa, polling sem bloqueio/pendência e quatro alarmes OK. Vercel Production `6486075247` corresponde ao mesmo commit. A conciliação posterior tem seus próprios gates e recibos no completion externo da etapa.
